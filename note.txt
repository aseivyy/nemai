;; This buffer is for text that is not saved, and for Lisp evaluation.
;; To create a file, visit it with ‘C-x C-f’ and enter text in its buffer.

read address of a element from a struct name:

save struct name
calculate offset to struct
calculate offset to element
donee, return in rax the address (no matter if there was a pointer accessed or not)



read element from a struct address (in rax):

calculate offset to element
donee, return in rax the address, equals rax plus offset (no matter if there was a pointer accessed or not)






sooo to translate
struct1->struct2.value

(strct-elem-addr value (strct-elem-name struct2 struct1))

struct1->struct2->value

(struct-elem-addr value (strct-elem-name struct2 struct1))


struct1->struct2->struct3->value





TO GET
struct1->struct2->struct3->value
assuming that all member structs are POINTERS

to get struct 2:

get address on the stack of struct1
calculate the offset
read data from calculated address, put into rax whatver is when you read from address specified in data

to get struct 3:
add offset to rax
read data at address in rax, put into rax whatver is when you read from address specified in data

to get value:
add offset to rax
return in rax whatever is at rax


SO I NEED:
get-struct-member-value-from-struct-name
get-struct-member-value-from-address

and I don't need more, since if specified member is a pointer, the value is an address, if not a pointer, then value is just a value :D
first function expects nothing as a child, no nesting, just struct name
second function, just expects an address in rax, don't care what is inside, the programmer actually need to think, not my compiler, it allows free will to do anything and is not police to check everything :D
aand some info about which element, from which struct definition, and this should be able to do everything
aaaaand add a function to get value from an address

(sp struct-name //size-type name//...) 
(deref size-type [expr])
(struct-element struct-name struct-element)
(struct-element struct-definition-name struct-element [expr])




MY THOUGHTS MIDWAY

siiince my code as said is not police, there are not a lot of types, by design there is planned b64, b32, b16, b8, u, s, f, "b" types are size types, to be used when needed as in struct memebrs, other ones are just for normal variables, for now only unsigned, "u" implemented, and these are just 64 bits all (maybeee except "f", I haven't researched much on floats yet), and the pointer is just an unsigned variable, no need for a literal night sky with pointers ^-^, so no ********my_pointer_yayy, aand if you would want to just dereference known address, you don't need entire casting from c, as it would be var = *((int64_t*) address) if I am right, in my language is just (av var (deref address)), the language just allows you to do whatever you want :D



if a person would want to just treat some memory as a struct, lets say 8 byte one with 2 4 byte elements, it's just (se structname low address), so simple, the user just have to keep eye, if he will get bad results and can't focus and find the error, then the language uses care and literally forces the user to get some rest, how awesome ^-^ 



all of design also allows for the most optimized printf, with incredible perfomance, even better than ones from the biggest corporations



(df printf_no_scam[]

    (ret) // return

    (ret)) // if first return fails make sure I don't do too much work and return again to be sure ^-^



yayy

